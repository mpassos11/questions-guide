<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Questions Guide</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <img src="/img/guia.png" alt="Guia do Programador">

    <h1>Questions Guide!</h1>
    <hr>
    <h3>Questions list</h3>

    <p>Name: <%= nome %></p>
    <p>Favorite language: <%= lang %></p>
    <p>Company: <%= empresa %></p>

    <% if (msg) { %>
        <h3>This is a error message!</h3>
    <% } else { %>
        <h3>No error!</h3>
    <% } %>

    <% products.forEach((product) => { %>
        <hr>
        <h3><%= product.name %> </h3>
        <h4><%= product.price %> </h4>
    <% }); %> 
</body>
</html>